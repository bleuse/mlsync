Test workflow for connectors/sympa.rb

login:
	success -> recupere un cookie valide
	fail -> raise exception

review:
	si pas session alors -> raise exception
	si session incorrecte -> raise exception
	si session correcte -> 
		si liste incorrecte (i.e., n'existe pas) -> raise exception
		sinon renvoie tableau des mails

add:
	si pas session alors -> raise exception
	si session incorrecte -> raise exception
	si session correcte -> 
		si liste incorrecte -> raise exception
		si liste correcte ->
			mail existant -> erreur
			mail nouveau ->
				si quiet -> ok & pas de mail & gecos == gecos
				si ¬quiet -> ok & mail & gecos == gecos
